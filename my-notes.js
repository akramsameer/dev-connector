/*
 *  -using proxy in package.json enabing you for calling 
 *   axios.get(/api/somthing) rather than writing the whole url
 *  
 *  - we use concurrently to run two command in the same time 
 *      run ther server and the client
 * 
 *  - Adding noValidate to form tag to prevent built-in validation for custom one
 */
